# EatSmart

Cleaned project structure:

- `backend/` — FastAPI backend API, models, logic, and Python dependencies.
- `frontend/` — static frontend files served by the backend.
- `docs/` — setup and deployment documentation.
- `.github/workflows/` — GitHub Actions workflow definitions.
- `Dockerfile` — container build configuration for deployment.
- `.gitignore` — repository ignore rules.
